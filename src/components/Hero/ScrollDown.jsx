import React from "react";
import { ScrollIcon } from "../../icons";

const ScrollDown = () => {
  return (
    <div className="hero__scroll">
      <a href="#about" className="hero__scroll-button">
        <ScrollIcon />
      </a>
    </div>
  );
};

export default ScrollDown;
